export interface newEventProps {
  nameEvent: string;
  local: string;
  start: string;
  finish: string;
  description: string;
  maxParticipants: number;
  social_label: string;
  sport_label: string;
  public?: boolean;
}
